name: showcase_clip_regular_polygon
description: An app showcasing the clip_regular_polygon package.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  clip_regular_polygon:
    git:
      url: git://github.com/JimGerth/clip_regular_polygon-flutter.git
      # TODO: Update this to master/stable, as soon as the
      # branch is merged.
      ref: feature/clip-widget

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
